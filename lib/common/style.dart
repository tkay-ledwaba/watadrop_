import 'package:flutter/material.dart';

// Color Scheme
var colorPrimary = Colors.white;
var colorSecondary = Colors.black;
var colorAccent = Colors.lightBlue;
var colorFont = Colors.black;
var colorSuccess = Colors.green;
var colorFailed = Colors.red;

// Border Style
var border_radius = BorderRadius.circular(0);

var text_field_padding = EdgeInsets.symmetric(horizontal: 8, vertical: 8);
